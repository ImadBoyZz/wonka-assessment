# Copy this file to .env.local and fill in the provided assessment keys.
# Keys are used server-side only (API routes) and are never exposed to the client.
ANTHROPIC_API_KEY=sk-ant-your-key-here
OPENAI_API_KEY=sk-your-key-here
